{
  "name": "engrain-tapi",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check": "tsc --noEmit",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "test": "bun test tests/*.test.ts",
    "test:watch": "bun test --watch tests/*.test.ts"
  },
  "dependencies": {
    "hono": "^4",
    "@engrain/telemetry": "workspace:*"
  },
  "devDependencies": {
    "wrangler": "^4",
    "@cloudflare/workers-types": "^4",
    "typescript": "^5"
  }
}
